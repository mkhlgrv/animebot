# Animebot

## Using

1. Install dependecies:

```bash
pip install -r requirements.txt
pip install -e .
```
2. Create an `.env` file from template `.envexample`:
```bash
touch .env
```
3. Run `main.ipynb`.
